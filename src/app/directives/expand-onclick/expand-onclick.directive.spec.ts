import { ExpandOnclickDirective } from './expand-onclick.directive';

describe('ExpandOnclickDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandOnclickDirective();
    expect(directive).toBeTruthy();
  });
});
